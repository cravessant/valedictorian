diff --git a/index.js b/index.js
index 4b6ae1ff0d9c82cfea7d135b5276f16609cbe8b0..8cfe6a107c36d060ba997742dded7c02204a73fe 100644
--- a/index.js
+++ b/index.js
@@ -9,6 +9,17 @@ var escClose = '\0CLOSE'+Math.random()+'\0';
 var escComma = '\0COMMA'+Math.random()+'\0';
 var escPeriod = '\0PERIOD'+Math.random()+'\0';

+// CVE-2026-14257 backport of juliangruber/brace-expansion@a1bd339, which only
+// shipped on the 5.x line. `max` caps the *number* of expansions but not their
+// length, so chaining brace groups (`'{a,b}'.repeat(1500)`) keeps the count
+// under `max` while every result grows with the number of groups, exhausting
+// memory and crashing the process with an uncatchable OOM. EXPANSION_MAX_LENGTH
+// bounds the total characters a single accumulator may hold, and expand() now
+// folds over top-level brace groups iteratively rather than recursing once per
+// group, so native stack depth stays constant too. The limit sits well above
+// any realistic expansion, so legitimate input is unaffected.
+var EXPANSION_MAX_LENGTH = 4000000;
+
 function numeric(str) {
   return parseInt(str, 10) == str
     ? parseInt(str, 10)
@@ -68,6 +79,7 @@ function expandTop(str, options) {

   options = options || {};
   var max = options.max == null ? Infinity : options.max;
+  var maxLength = options.maxLength == null ? EXPANSION_MAX_LENGTH : options.maxLength;

   // I don't know why Bash 4.3 does this, but it does.
   // Anything starting with {} will have the first two bytes preserved
@@ -79,7 +91,7 @@ function expandTop(str, options) {
     str = '\\{\\}' + str.substr(2);
   }

-  return expand(escapeBraces(str), max, true).map(unescapeBraces);
+  return expand(escapeBraces(str), max, maxLength, true).map(unescapeBraces);
 }

 function identity(e) {
@@ -100,15 +112,91 @@ function gte(i, y) {
   return i >= y;
 }

-function expand(str, max, isTop) {
-  var expansions = [];
+// Build `acc[a] + pre + values[v]` for every combination, capping the number of
+// results at `max` and the total number of characters at `maxLength`. This is
+// the one place output grows, so bounding it here keeps the single accumulator
+// - and therefore memory - flat regardless of how many brace groups combine.
+function combine(acc, pre, values, max, maxLength, dropEmpties) {
+  var out = [];
+  var length = 0;
+  for (var a = 0; a < acc.length; a++) {
+    for (var v = 0; v < values.length; v++) {
+      if (out.length >= max) return out;
+      var expansion = acc[a] + pre + values[v];
+      // Bash drops empty results at the top level. Skip them before they count
+      // against `max`, so `max` bounds the number of *kept* results.
+      if (dropEmpties && !expansion) continue;
+      if (length + expansion.length > maxLength) return out;
+      out.push(expansion);
+      length += expansion.length;
+    }
+  }
+  return out;
+}
+
+// The expansion values of a single numeric (`1..5`) or alphabetic (`a..e..2`)
+// sequence body.
+function expandSequence(body, isAlphaSequence, max) {
+  var n = body.split(/\.\./);
+  var N = [];
+  var x = numeric(n[0]);
+  var y = numeric(n[1]);
+  var width = Math.max(n[0].length, n[1].length);
+  var incr = n.length == 3
+    ? Math.max(Math.abs(numeric(n[2])), 1)
+    : 1;
+  var test = lte;
+  var reverse = y < x;
+  if (reverse) {
+    incr *= -1;
+    test = gte;
+  }
+  var pad = n.some(isPadded);
+
+  for (var i = x; test(i, y) && N.length < max; i += incr) {
+    var c;
+    if (isAlphaSequence) {
+      c = String.fromCharCode(i);
+      if (c === '\\')
+        c = '';
+    } else {
+      c = String(i);
+      if (pad) {
+        var need = width - c.length;
+        if (need > 0) {
+          var z = new Array(need + 1).join('0');
+          if (i < 0)
+            c = '-' + z + c.slice(1);
+          else
+            c = z + c;
+        }
+      }
+    }
+    N.push(c);
+  }
+  return N;
+}
+
+function expand(str, max, maxLength, isTop) {
+  // Consume the string's top-level brace groups left to right, threading a
+  // running set of combined prefixes (`acc`). Expanding the tail iteratively -
+  // rather than recursing on `m.post` once per group - keeps native stack depth
+  // constant, and leaves a single accumulator whose size `maxLength` bounds.
+  var acc = [''];
+
+  // Bash drops empty results, but only when the *first* top-level group is a
+  // comma set - a sequence may legitimately yield ''. The drop applies to the
+  // final strings, so it is passed to whichever combine() produces them.
+  var dropEmpties = false;
+  var firstGroup = true;

-  // The `{a},b}` rewrite below restarts expansion on a rewritten string with
-  // the same `max` and `isTop = true`. Loop instead of recursing so a long run
-  // of non-expanding `{}` groups can't exhaust the call stack.
   for (;;) {
     var m = balanced('{', '}', str);
-    if (!m || /\$$/.test(m.pre)) return [str];
+
+    // No brace set left, or a `${...}` group: on this line the rest of the
+    // string stays literal, including any tail after the dollar group.
+    if (!m || /\$$/.test(m.pre))
+      return combine(acc, str, [''], max, maxLength, dropEmpties);

     var isNumericSequence = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(m.body);
     var isAlphaSequence = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(m.body);
@@ -121,89 +209,41 @@ function expand(str, max, isTop) {
         isTop = true
         continue
       }
-      return [str];
+      // Nothing here expands, so the whole remaining string is literal.
+      return combine(acc, str, [''], max, maxLength, dropEmpties);
     }

-    var n;
-    if (isSequence) {
-      n = m.body.split(/\.\./);
-    } else {
-      n = parseCommaParts(m.body);
-      if (n.length === 1) {
-        // x{{a,b}}y ==> x{a}y x{b}y
-        n = expand(n[0], max, false).map(embrace);
-        if (n.length === 1) {
-          var post = m.post.length
-            ? expand(m.post, max, false)
-            : [''];
-          return post.map(function(p) {
-            return m.pre + n[0] + p;
-          });
-        }
-      }
+    if (firstGroup) {
+      dropEmpties = isTop && !isSequence;
+      firstGroup = false;
     }

-    // at this point, n is the parts, and we know it's not a comma set
-    // with a single entry.
-
     // no need to expand pre, since it is guaranteed to be free of brace-sets
     var pre = m.pre;
-    var post = m.post.length
-      ? expand(m.post, max, false)
-      : [''];

     var N;
-
     if (isSequence) {
-      var x = numeric(n[0]);
-      var y = numeric(n[1]);
-      var width = Math.max(n[0].length, n[1].length)
-      var incr = n.length == 3
-        ? Math.max(Math.abs(numeric(n[2])), 1)
-        : 1;
-      var test = lte;
-      var reverse = y < x;
-      if (reverse) {
-        incr *= -1;
-        test = gte;
-      }
-      var pad = n.some(isPadded);
-
-      N = [];
-
-      for (var i = x; test(i, y) && N.length < max; i += incr) {
-        var c;
-        if (isAlphaSequence) {
-          c = String.fromCharCode(i);
-          if (c === '\\')
-            c = '';
-        } else {
-          c = String(i);
-          if (pad) {
-            var need = width - c.length;
-            if (need > 0) {
-              var z = new Array(need + 1).join('0');
-              if (i < 0)
-                c = '-' + z + c.slice(1);
-              else
-                c = z + c;
-            }
-          }
+      N = expandSequence(m.body, isAlphaSequence, max);
+    } else {
+      var n = parseCommaParts(m.body);
+      if (n.length === 1) {
+        // x{{a,b}}y ==> x{a}y x{b}y
+        n = expand(n[0], max, maxLength, false).map(embrace);
+        if (n.length === 1) {
+          acc = combine(acc, pre + n[0], [''], max, maxLength, dropEmpties && !m.post.length);
+          if (!m.post.length) break;
+          str = m.post;
+          continue;
         }
-        N.push(c);
       }
-    } else {
-      N = concatMap(n, function(el) { return expand(el, max, false) });
-    }

-    for (var j = 0; j < N.length; j++) {
-      for (var k = 0; k < post.length && expansions.length < max; k++) {
-        var expansion = pre + N[j] + post[k];
-        if (!isTop || isSequence || expansion)
-          expansions.push(expansion);
-      }
+      N = concatMap(n, function(el) { return expand(el, max, maxLength, false) });
     }

-    return expansions;
+    acc = combine(acc, pre, N, max, maxLength, dropEmpties && !m.post.length);
+    if (!m.post.length) break;
+    str = m.post;
   }
+
+  return acc;
 }
